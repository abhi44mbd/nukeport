# create a file
echo "# killport" > README.md

# add it
git add README.md

# commit it
git commit -m "initial commit"

# rename branch to main (optional but recommended)
git branch -M main

# push
git push -u origin main